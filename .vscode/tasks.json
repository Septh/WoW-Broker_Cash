{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "FindGlobals.lua",
            "type": "shell",
            "command": "luac-wow -l -p '${file}' | lua-wow /e/lua/globals.lua '${file}'"
        }
    ]
}
